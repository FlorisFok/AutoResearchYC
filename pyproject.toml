[project]
name = "yc-bench-prompt-evolution"
version = "0.1.0"
description = "Autonomous prompt evolution for YC-Bench — #1 on the leaderboard"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
